{% extends 'base.html.twig' %}

{% block title %}TrickCategory{% endblock %}

{% block body %}
    <h1>TrickCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trick_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trick_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_debug_trick_category_index') }}">back to list</a>

    <a href="{{ path('app_debug_trick_category_edit', {'id': trick_category.id}) }}">edit</a>

    {{ include('debug_trick_category/_delete_form.html.twig') }}
{% endblock %}
